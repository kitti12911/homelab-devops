apiVersion: longhorn.io/v1beta2
kind: RecurringJob
metadata:
  name: snapshot-cleanup
  namespace: longhorn-system
spec:
  cron: "0 */6 * * *"
  task: snapshot-cleanup
  groups:
    - default
  retain: 0
  concurrency: 1
